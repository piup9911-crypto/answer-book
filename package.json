{
  "name": "aqi-answer-book",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "vendor": "node scripts/copy-three.mjs",
    "dev": "wrangler dev",
    "check": "npm run vendor && node scripts/check.mjs",
    "deploy": "npm run check && wrangler deploy",
    "deploy:dry-run": "npm run check && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}

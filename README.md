# 阿祈的答案之书

一个独立部署的互动答案之书，包含 412 条答案和两种开启方式：

- 触碰开启：轻触封面，随机翻到一个答案。
- 滑动翻页：用手指或鼠标连续翻页，松手后停在当前答案。

## 本地运行

```powershell
npm install
npm run dev
```

## 检查与部署

```powershell
npm run check
npm run deploy:dry-run
npm run deploy
```

部署目标为 `answer-book.naginoumi.com`。
